# Custom partition table maximizing firmware size for the M5Stack Cardputer (ESP32-S3).
# This layout sacrifices OTA slots and filesystem storage so the factory app can
# use essentially the entire 8MB flash (minus the 64KB bootloader region).
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  factory, 0x10000,  0x7F0000,
